name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]  # Solo utilizaremos Node.js 20.x

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v5  # Usamos la versión v5 de setup-node para Node.js 20
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      - run: npm run build
      - run: npm test

